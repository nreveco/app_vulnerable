# Security Configuration - Secrets Fix
# Hardcoded Secrets - Generated: 2025-10-20T21:08:12.366Z

# Database configuration (use environment variables)
spring.datasource.url=${DB_URL:jdbc:h2:mem:testdb}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

# JWT Configuration (use environment variables)
jwt.secret=${JWT_SECRET:your-secret-key-here}
jwt.expiration=${JWT_EXPIRATION:86400}

# API Keys (use environment variables)
api.key=${API_KEY:your-api-key-here}
encryption.key=${ENCRYPTION_KEY:your-encryption-key-here}

# Security Notes:
# 1. Always use environment variables or secret managers for sensitive data instead of hardcoding them in your source code.
2. Regularly review and update your project's environment variables or secret managers to ensure they remain secure.
3. Consider implementing additional security measures, such as encryption or access controls, to protect sensitive data.
# Never commit sensitive values to version control
# Use proper secret management systems in production
